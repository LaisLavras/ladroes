/// <reference types="vite/client" />

// Injected at build time by vite.config.ts (git commit the bundle was built
// from) — see MenuScene's build-stamp text.
declare const __BUILD_COMMIT__: string;
declare const __BUILD_TIME__: string;
